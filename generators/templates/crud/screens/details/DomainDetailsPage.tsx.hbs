"use client"; import { Box, Head } from "@/shared/ui";
import { {{pascalCase name}}Details } from "@/slices/appointments/entidades/{{camelCase name}}/details";
import { {{pascalCase name}}Props } from "@/slices/appointments/entidades/{{camelCase name}}";
import { useTranslation } from "react-i18next";

type {{pascalCase name}}DetailsProps = {
  data: {{pascalCase name}}Props;
  id: string;
};
export const {{pascalCase name}}DetailsPage = ({ data }: {{pascalCase name}}DetailsProps) => {
  const { t } = useTranslation("PAGES");
  const props = { {{camelCase name}}: data };
  return (
    <>
      <Head
        title={"Belezix Admin | {{pascalCase label}}s"}
        description="Página de detalhes de {{camelCase label}}s do painel de Admin Belezix"
      />
       <Box flex="1" borderRadius={8} bg="secondary.500" p="8">
        <{{pascalCase name}}Details {...props} />
      </Box>
    </>
  );
};
